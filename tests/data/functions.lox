fun fib(limit) {
  var a = 0;
  var temp;

  for (var b = 1; a < limit; b = temp + b) {
    print a;
    temp = a;
    a = b;
  }
}

fib(10000);

fun add(a, b) {
  return a + b;
}

assert(add(1, 2) == 3, "add function should work correctly");